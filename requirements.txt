# Pinned to the versions the results in the dissertation were produced with.
# Unpinned ranges let each rebuild resolve to whatever is newest, which broke
# the deployment once; exact pins also make the run reproducible.
streamlit==1.63.0
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
pandas==3.0.5
numpy==2.5.2
joblib==1.5.3
matplotlib==3.11.1
tldextract==5.3.2
python-whois==0.9.6
dnspython==2.8.0
Pillow==12.3.0
pytesseract==0.3.13
